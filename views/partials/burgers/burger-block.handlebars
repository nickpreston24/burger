<li>
    {{name}}

    <button class="change-devoured" data-id="{{id}}" data-newdevoured="{{devour}}">
        {{#if devour}}Nom Nom!{{else}}Naw, full!{{/if}}
    </button>

    <button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>